# Goal 模式短输入模板

把下面这段直接复制到 goal 模式即可。

```text
请接手当前仓库并完成最终交付。不要只做分析，必须真实修改、启动、修复、回归、做双端端到端测试，直到系统达到我可以直接使用的状态。

先完整阅读以下文件：
- GOAL_SPEC.md
- ACCEPTANCE.md
- RUNBOOK.md
- OPTIMIZATION_REPORT.md
- 示例地区报销智能平台-完整方案.md

执行要求：
- 以当前代码、当前数据库、当前脚本、当前运行状态为准，不要假设历史结论仍然成立
- 必须真实启动 backend、web、mobile h5，并验证端口、页面、接口、日志
- 必须覆盖后端业务链、Web 管理端、Mobile H5、OCR 真服务、测试数据、回归脚本、统一报告
- 不允许只做构建通过；必须完成 API smoke、Web Playwright、Mobile H5 Playwright
- 不允许清库重置；只允许增量修复数据
- 不允许破坏现有后端 API 路径与语义；只能修 bug 或做兼容增强
- 主界面不要散落解释性文案，操作说明只保留在头像下拉中的隐藏说明页
- 更新现有 canonical 文档，不新增同主题平行总结文档
- 全部验收通过后，再宣布完成

最终只有在以下条件全部满足后才算完成：
- 后端业务正确
- Web 六门户可直接使用
- Mobile H5 四 Tab 可直接使用
- OCR 真服务或 fallback 可真实工作
- 核心业务链闭环跑通
- 统一回归脚本全绿
- 我拿到可直接访问的地址、账号、报告目录，并能直接使用系统
```

## 使用方法

如果 goal 输入长度受限，不要把全部需求塞进 goal 本身，而是：

1. 在 goal 里只放上面的短输入  
2. 把长需求写进 `GOAL_SPEC.md`、`ACCEPTANCE.md`、`RUNBOOK.md`  
3. 让执行者先读文件，再按文件执行

这样既不会超过 goal 的长度限制，也能保证要求完整。
