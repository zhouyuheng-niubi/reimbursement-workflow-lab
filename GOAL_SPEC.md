# 示例地区智能报销平台最终交付目标

## 1. 总目标

把当前仓库收口到以下状态：

- 后端业务正确
- Web 管理端可直接使用
- Mobile H5 可直接使用
- OCR 真服务可用，或 fallback 明确可用
- 核心业务链闭环可跑
- 全量端到端验证有证据
- canonical 文档已更新
- 最终系统已启动并可由业务方直接使用

## 2. 当前仓库事实边界

- 仓库路径：`<LOCAL_PATH>`
- 当前交付形态：
  - backend：Spring Boot
  - web：Vue3 + Vite + Ant Design Vue
  - mobile：uni-app H5
- 本轮不扩展到微信真机交付
- 不跨 repo 借实现

## 3. 不可妥协的约束

- 不允许只做分析或计划，必须真实修改、运行、测试、回归
- 不允许清库、重建库、重置环境
- 只允许增量修复测试数据
- 不允许破坏现有后端 API 路径与语义
- 可以做兼容增强、补展示字段、补前端适配层
- 主界面不保留多余解释性文案
- 操作说明只允许存在于头像下拉中的隐藏说明页
- 更新现有 canonical 文档，不新增同主题平行总结文档

## 4. 必须完成的系统范围

### 4.1 后端

必须复核并补稳以下链路：

- 登录、token、当前用户信息、深链访问一致性
- OCR 状态、识别、发票夹查询、`mineOnly` 作用域
- 报销创建、保存草稿、直接提交、撤回、驳回、重提
- 审批 FAIL-CLOSED、待审批可见性、转审、核定金额、审批意见
- 付款生成、确认付款、付款失败、退款回退、`paymentAt` 与主单状态同步
- 报表汇总、系统配置 CRUD、组织/项目主数据读取

### 4.2 Web 管理端

必须最终收口为六门户：

- 我的工作台
- 我要报销
- 我要审批
- 我要付款
- 治理配置
- 数据与日志

并确保以下主链连续：

- 员工链：拍票识别 -> 我的发票夹 -> 新建报销 -> 我的报销 -> 报销详情
- 审批链：待办列表 -> 单据摘要/发票/OCR/风险 -> 审批动作区
- 财务链：待付款汇总 -> 付款列表 -> 确认付款/失败/退款

### 4.3 Mobile H5

必须最终收口为四 Tab：

- 首页
- 报销
- 审批
- 我的

并确保以下高频链可跑：

- 拍票识别 -> 识别确认 -> 存入发票夹或直接提单 -> 提交报销 -> 跟踪状态
- 待我审批列表 -> 审批详情 -> 通过/驳回/转审

### 4.4 OCR

必须满足：

- 优先走阿里云专用票据识别
- fallback 为 Qwen OCR
- 双端页面能显示 provider / fallback
- 真服务异常时不允许静默失败

## 5. 文案与交互原则

- 页面只保留标题、字段名、状态、数字、动作按钮
- 可以保留必要运行态信息：
  - OCR 未启用原因
  - 风险/异常提示
  - 必需校验提示
- 不允许保留“教学说明式”页面文案
- 新用户进入系统后 30 秒内必须知道第一步做什么

## 6. 完成定义

只有在以下条件全部满足后，才算完成：

- 后端业务正确
- Web 六门户可操作
- Mobile H5 四 Tab 可操作
- OCR 可真实使用
- API smoke 全绿
- Web Playwright 全绿
- Mobile H5 Playwright 全绿
- canonical 文档已更新
- 服务已启动，地址明确，可直接使用
