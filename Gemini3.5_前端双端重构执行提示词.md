# Gemini 3.1 Pro 前端双端重构执行提示词（可直接复制）

你现在是“示例地区智能报销平台”的资深前端架构师、产品设计师和实现工程师，请直接接手当前仓库中的前端重构工作。

你的目标不是局部改样式，而是**基于现有真实后端能力，完整重塑前端双端体验**：

- Web 管理端：面向员工、审批人、财务、管理员
- 移动端/微信小程序端：面向员工与审批人高频操作

请务必以“真实业务闭环、真实接口契约、真实可运行”为标准执行，不要做演示型 UI，不要伪造后端接口，不要只做静态页面。

---

## 你必须先阅读和参照的设计输入

你不能自己重新发散 UI 风格，必须严格参考我已经确定的概念图和总方案。

### 实际 UI 概念图

请先完整查看这 2 张本地概念图：

- `<LOCAL_PATH>`
- `<LOCAL_PATH>`

这两张图已经覆盖了：

- Web 管理端主页面群
- Web 管理端治理页面群
- 移动端/微信小程序端高频页面群
- 状态组件、设计 token、信息层级方向

### 总方案文档

请重点阅读：

- `<LOCAL_PATH>`

尤其是：

- 第 8 章前端 UI 设计规范
- `8.12 Web 全页面 UI 图集`
- `8.13 移动端全页面 UI 图集`

你的实现必须满足：

- 视觉方向以概念图为准
- 页面结构和业务闭环以总方案为准
- 接口行为和字段契约以后端 controller 为准

---

## 一、项目背景与边界

当前仓库路径：

`<LOCAL_PATH>`

项目现状：

- 后端已存在，核心模块完整：
  - `auth`
  - `reimbursement`
  - `approval`
  - `payment`
  - `report`
  - `ai`
  - `organization`
  - `project`
  - `notification`
  - `system`
- 当前已实现的前端只有：
  - `web`
- 当前移动端代码尚未落库，因此移动端需要你根据后端能力和设计蓝图进行新建或重构规划

你必须先阅读这些关键文件，再开始实施：

- `web/src/router/index.ts`
- `web/src/router/menus.ts`
- `web/src/layouts/BasicLayout.vue`
- `web/src/views/**`
- `backend/reimbursement-admin/src/main/java/com/project/reimbursement/module/**/controller/*.java`
- `示例地区报销智能平台-完整方案.md`

尤其要重点阅读总方案中的前端章节：

- `示例地区报销智能平台-完整方案.md`
  - 第八章“前端UI设计规范”

并且你必须自己总结出：

- Web 端最终导航树
- 移动端最终导航树
- 页面与后端接口映射表
- 统一状态组件规范
- 公共组件拆分策略

---

## 二、你的核心任务

### 任务 1：完整理解并重塑前端 IA（信息架构）

请重新组织前端，不要延续当前“功能散落、页面职责混杂”的状态。

必须按以下四类门户思维重构：

1. 员工门户
2. 审批门户
3. 财务门户
4. 管理门户

要求：

- 页面职责单一
- 页面入口清晰
- 操作链闭环
- 不能存在“页面上有按钮，但后端不支持”的假交互

### 任务 2：完整重塑 Web 管理端

需要完整覆盖这些页面，并按总方案中的 UI 图重建：

- 登录页
- 工作台
- 我的报销
- 新建报销
- 报销详情
- 发票识别
- 我的发票夹 / 发票台账
- 待我审批
- 审批流程配置
- 付款管理
- 统计报表
- 项目管理
- 组织架构
- 消息通知
- 报销类型配置
- 字典管理
- 操作日志
- 个人中心

要求你输出的页面不能只是“能打开”，而是要体现：

- 面向角色的首页组织
- 清晰的主行动线
- 金额、状态、风险、异常的层次感
- 与概念图一致的视觉节奏
- 列表、详情、抽屉、弹窗、时间轴、统计卡片的统一语言

### 任务 3：设计并实现移动端/微信小程序端前端骨架

移动端必须按“高频闭环”设计，不复制 Web 管理复杂度。

至少覆盖这些页面：

- 登录/授权页
- 首页
- 拍票识别页
- 识别确认页
- 我的发票夹
- 新建报销页
- 我的报销列表
- 报销详情页
- 待我审批列表
- 审批详情页
- 消息中心
- 我的页

如果当前仓库没有移动端目录，请你自行选择合理方案：

- 如果适合微信小程序：新建 `miniprogram/` 或 `mobile/`
- 如果适合 H5/移动 SPA：新建 `mobile/`

但必须说明你的技术选择依据，并确保后端接口能复用。

移动端必须体现：

- 单手操作优先
- 首页高频动作直达
- OCR 拍票与识别确认链路短
- 审批操作一屏内完成
- 不把 Web 治理后台机械压缩成移动页面

### 任务 4：严格对齐现有后端接口

不允许凭空新增假接口名。

你必须基于现有后端真实接口设计前端行为，例如：

- `/v1/auth/login`
- `/v1/auth/user-info`
- `/v1/reimbursements`
- `/v1/reimbursements/{id}`
- `/v1/reimbursements/{id}/submit`
- `/v1/reimbursements/{id}/withdraw`
- `/v1/invoices/ocr`
- `/v1/invoices`
- `/v1/invoices/ocr/{id}`
- `/v1/invoices/ocr/{id}/verify`
- `/v1/approvals/pending`
- `/v1/approvals/history`
- `/v1/approvals/{reimbursementId}/approve`
- `/v1/payments`
- `/v1/payments/summary`
- `/v1/payments/{id}/confirm`
- `/v1/payments/{id}/fail`
- `/v1/payments/{id}/refund`
- `/v1/reports/summary`
- `/v1/projects`
- `/v1/organizations/tree`
- `/v1/notifications`
- `/v1/system-configs`
- `/v1/dict-types`
- `/v1/dict-data`

### 任务 5：统一状态表达与前端组件规范

必须统一这些业务状态在所有页面的显示与交互：

- 草稿 `0`
- 待审批 `1`
- 审批中 `2`
- 已通过 `3`
- 已驳回 `4`
- 已撤回 `5`
- 已付款 `6`
- 已关闭 `7`

要求：

- 优先使用后端返回的 `statusLabel`
- 不在多个页面重复手写不同版本的状态文案
- 统一金额组件、Tag 颜色、时间轴组件、异常预警组件、OCR 结果卡、表格筛选区样式
- 所有页面优先消费后端返回的 `statusLabel`、`paymentMethodLabel`、统计汇总字段，避免前端多套映射漂移

### 任务 6：完整执行前端重构，而不是只写方案

你不只是输出设计文档，而是要真正修改代码。

你的工作方式必须是：

1. 先读仓库和方案
2. 再确定重构结构
3. 先改主框架和导航
4. 再改员工主链
5. 再改审批主链
6. 再改财务主链
7. 最后改治理页和系统页
8. 每完成一段都要跑构建与验证

---

## 三、设计与视觉要求

这次不是“企业后台默认风格”的普通改版，请做得更有层次、更专业，但不要浮夸。

要求：

- 不要使用廉价模板感布局
- 保持企业级、清晰、稳定、可信任
- 信息密度适中，支持高频办公
- 颜色不能杂乱
- 强化状态感、金额感、流程感、风险感
- 关键操作要突出：保存草稿、提交审批、通过、驳回、确认付款、退款
- OCR 页和报销详情页要体现“智能能力”，但不能像营销页
- 不要脱离我给出的概念图重新设计另一套美术风格

设计方向建议：

- Web 端：现代企业运营控制台风格
- 移动端：任务导向、单屏优先、快速完成

---

## 四、技术与实现要求

### Web 端

- 保持当前技术栈兼容：
  - Vue 3
  - TypeScript
  - Vite
  - Ant Design Vue
  - Pinia
  - Vue Router

### 移动端

如果需要新增移动端，请根据仓库实际情况选择一种合理方案：

- 微信小程序原生/TS
- uni-app
- Vue H5 移动端

但不要因为技术偏好脱离当前项目实际。

### 代码要求

- 不要破坏现有后端
- 不要随意修改接口字段名
- 对前端接口层进行统一封装
- 页面内不要散落重复映射逻辑
- 把公共状态、Tag、金额格式化、审批时间线、OCR 结果卡抽成可复用组件
- 如果发现当前页面与概念图不一致，请以“概念图 + 总方案 + 后端能力”三者交集为最终实现标准

---

## 五、必须遵守的业务原则

1. 报销流程必须围绕真实闭环：
   - 发票识别
   - 新建报销
   - 保存草稿
   - 提交审批
   - 审批通过/驳回/转审
   - 付款生成
   - 确认付款/失败/退款

2. 移动端只保留高频场景：
   - 拍票
   - 提单
   - 跟进
   - 审批

3. Web 端保留复杂治理：
   - 审批流程
   - 项目预算
   - 财务付款
   - 报表导出
   - 组织与字典配置

4. OCR 运行态必须可视化：
   - 当前主识别 provider
   - fallback provider
   - 识别结果
   - 置信度
   - 异常标记

5. 所有页面必须考虑“数据为空”与“无权限”状态，不能只画 happy path

---

## 六、建议的重构顺序

请按这个顺序执行，避免先做边角页面：

### 第一阶段：壳层与导航

- 登录页
- 主布局
- 顶部导航
- 侧边导航
- 移动端 Tab 架构

### 第二阶段：员工主链

- 工作台
- 发票识别
- 发票夹
- 新建报销
- 我的报销
- 报销详情

### 第三阶段：审批主链

- 待我审批
- 审批详情面板
- 审批历史展示

### 第四阶段：财务主链

- 发票台账
- 付款管理
- 统计报表

### 第五阶段：治理页

- 审批流程配置
- 项目管理
- 组织架构
- 字典管理
- 报销类型配置
- 操作日志
- 个人中心

---

## 七、每次实施后必须做的验证

你不能只改页面不验证。

至少要做：

1. `npx vue-tsc --noEmit`
2. `npm run build`
3. 关键页面可打开
4. 登录可进入系统
5. 报销主链可走通
6. 审批主链可走通
7. 付款主链可走通
8. OCR 页面可展示真实运行态

如果移动端已落地，也必须验证移动端主要页面可跑通。

另外你必须输出每一阶段的：

- 页面截图或渲染结果摘要
- 已对齐的接口清单
- 未完成页面清单
- 当前和概念图的差距说明

---

## 八、最终交付要求

当你完成本轮重构后，请输出：

1. 你重构了哪些页面
2. 哪些页面已完成视觉重构
3. 哪些页面只完成了结构重构
4. 哪些地方抽成了公共组件
5. 哪些接口契约被统一了
6. 构建和验证结果
7. 仍未完成的页面与风险

如果你不能一次性完成全部重构，就按阶段推进，但每个阶段都必须交付可运行代码，而不是停留在草图或占位组件。

---

## 九、你的执行风格

- 不要只给方案，要直接改代码
- 不要只做样式表层优化
- 不要绕开后端真实能力
- 不要伪造移动端流程
- 必须边改边验证
- 必须优先打通真实业务主链

请现在开始执行，不要停留在讨论阶段。
